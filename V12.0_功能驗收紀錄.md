# V12.0 功能驗收紀錄

⚠️ 說明：原始指令「二十一、正式驗收項目」列出 30 個編號項目，但完整逐字內容在本次會話的上下文壓縮過程中沒有保留原始編號與逐字文字，只保留了範圍摘要。本文件依照摘要範圍與指令其餘 22 個章節的具體要求，重建一份涵蓋相同範圍的 30 項驗收清單；如果使用者手上的原始指令文件與本文件的項目順序/編號不完全一致，請以原始指令文件的編號為準，本文件的驗收結論（通過/未完成實測）仍然適用於對應的功能點。

驗收方式共三種，逐項標示：
- 【程式碼審查】：比對程式碼邏輯與 Prisma schema 真實欄位/關聯，確認邏輯正確，但不是實際執行。
- 【沙盒內真實執行】：tsx 直接執行純邏輯測試，或對真實 PostgreSQL 資料庫執行 SQL 驗證。
- 【未完成實測】：需要瀏覽器操作、npm 建置、或正式環境，本沙盒無法執行。

| # | 驗收項目 | 方式 | 結果 |
|---|---|---|---|
| 1 | 信眾關係中心可從首頁正常開啟 | 未完成實測 | 需要瀏覽器操作，程式碼已就緒（`DevoteeCenterHomeCard`＋首頁連結）。 |
| 2 | 信眾名單資料來源為真實 `Member`，非另一套獨立名單 | 程式碼審查 | `listDevotees()` 直接查詢 `prisma.member.findMany()`，確認無誤。 |
| 3 | 信眾名單搜尋（姓名/電話/地址/家戶編號/主要聯絡人/公司/Email/LINE/標籤）皆正確 | 程式碼審查 | `devoteeList.ts` 的 `OR` 條件逐一比對欄位名稱與 schema 相符。 |
| 4 | 信眾名單 14 種篩選條件邏輯正確 | 程式碼審查 | 逐一比對 `DevoteeListFilter` 的 14 個 case，含「本月生日」的兩階段（DB 粗篩＋應用層精篩，僅作用於分頁後的固定筆數）。 |
| 5 | 信眾名單分頁與每頁筆數正確 | 程式碼審查 | `skip`/`take` 計算與 `MAX_PAGE_SIZE=100` 上限確認正確。 |
| 6 | 360°總覽「基本資料」正確整合 Member+Household+DevoteeProfile | 沙盒內真實執行 | `composeDevoteeSummary()` 欄位對照 schema 逐一確認；已用 SQL 確認關聯欄位存在。 |
| 7 | 360°總覽「宮務活動紀錄」正確（不含祭改） | 程式碼審查 | `getRitualRecordHistory()` 排除 `activityType !== "PURIFICATION"`，確認邏輯正確。 |
| 8 | 360°總覽「祭祀及祭改紀錄」正確 | 程式碼審查 | `getPurificationHistory()` 直接查 `PurificationEntry.memberId`（獨立欄位，非透過 `RitualRecord.memberId`），已確認 schema 支援此查詢路徑。 |
| 9 | 360°總覽「供品認捐紀錄」正確 | 程式碼審查 | 重用既有 `getMemberOfferingHistory()`，未重寫查詢邏輯。 |
| 10 | 360°總覽「收款紀錄」「收據紀錄」正確 | 程式碼審查 | 直接查 `PaymentTransaction.payerMemberId` / `Receipt.memberId`。 |
| 11 | 捐款統計只計算實收金額，未收金額不計入實收 | 沙盒內真實執行（邏輯審查+SQL欄位確認） | `sumInYears()` 明確分開 `received`（`amountPaid`）與 `due`（`amountDue`），`unpaid = max(0, due - received)`，未把應收誤算實收。 |
| 12 | 應收/實收/未收分開顯示 | 程式碼審查 | `DevoteeSearchResult`/`DonationStats` 型別皆為三個獨立欄位，前端 360 頁面分三張卡片顯示。 |
| 13 | 時間軸只引用真實資料，無重複複製 | 程式碼審查 | `buildTimeline()` 純粹重新標記已查詢過的各區塊資料，沒有任何 `create`/`insert`，也沒有重新查詢一次相同資料。 |
| 14 | 時間軸無重複項目 | 程式碼審查 | 每個區塊（活動/祭改/供品/收款/收據/互動）各自只被加入一次，來源資料本身沒有重複列。 |
| 15 | 新增互動紀錄功能正常運作 | 未完成實測（介面）/ 程式碼審查（邏輯） | `POST /api/devotee-center/[memberId]/interactions` 已串接前端表單，實際點擊操作未測。 |
| 16 | 套用標籤功能正常運作 | 未完成實測（介面）/ 程式碼審查（邏輯） | `POST /api/devotee-center/[memberId]/tags` 已串接前端（`TagPicker`），實際點擊操作未測。 |
| 17 | 同一標籤不得重複套用 | 沙盒內真實執行 | 已用真實 PostgreSQL 對 `(devoteeProfileId, tagId)` 唯一索引插入重複資料，確認資料庫拒絕。 |
| 18 | READONLY 權限邊界（僅查看） | 沙盒內真實執行 | `verify_devotee_permissions.ts`（94 項 tsx 執行斷言）確認 READONLY 無法通過任何寫入類 action 檢查。 |
| 19 | ADMIN 權限邊界（不可管理系統權限/合併/刪除財務資料/敏感設定） | 沙盒內真實執行 | 同上，確認 ADMIN 只有 `view`/`viewFinancialSummary`/`updateProfile`/`applyTag`/`createInteraction`。 |
| 20 | SUPER_ADMIN 擁有完整權限 | 沙盒內真實執行 | 同上，確認 SUPER_ADMIN 擁有除 `mergeDevotees` 外的全部 action（`mergeDevotees` 本輪不開放給任何角色，見第 30 項）。 |
| 21 | 已往生信眾預設不出現在生日提醒 | 程式碼審查 | `eligibleMembersBase()` 明確 `where: { isDeceased: false }`。 |
| 22 | 已往生信眾歷史資料完整保留（不從家戶/活動/收款/收據紀錄消失） | 程式碼審查 | 360 總覽各區塊查詢條件沒有排除 `isDeceased`，只有生日提醒/名單「在用」篩選會排除，資料本身未被過濾掉。 |
| 23 | 停用信眾歷史資料完整性 | 程式碼審查 | `isDisabled` 只影響 `DevoteeProfile` 本身與生日提醒排除，不影響既有 `RitualRecord`/`PaymentTransaction`/`Receipt` 資料的可見性。 |
| 24 | 疑似重複信眾僅列出建議，無自動合併 | 沙盒內真實執行 | `devoteeDuplicateMatcher.ts` 沒有任何合併函式；`verify_devotee_duplicate_matcher.ts`（13 項斷言）確認比對邏輯正確且不誤判；頁面已含強制文案「疑似重複僅供人工確認，系統不會自動刪除或合併資料。」 |
| 25 | 需要關懷名單：系統只建議、不自動正式標記 | 程式碼審查 | `listSuggestedCareCandidates()` 回傳建議清單，不寫入 `careFlag`；只有管理者呼叫 `flagDevoteeForCare()` 才會寫入。 |
| 26 | 名單/搜尋分頁與效能設計（索引/批次查詢/避免 N+1） | 程式碼審查 | 見 `V12.0_效能檢查紀錄.md`，實際負載未量測。 |
| 27 | 既有 V10.0/V10.1/V11.0～V11.2.1 功能未被破壞 | 程式碼審查 | 本輪除 `schema.prisma`（純新增 model/欄位）、`permissions.ts`（純新增章節）、`page.tsx`（新增卡片與連結）外，沒有修改任何既有檔案；既有 lib/API/頁面檔案內容逐字未動。 |
| 28 | Google Drive 備份功能不受影響 | 程式碼審查 | 本輪完全未觸碰 `src/lib/googleDrive.ts`／`src/lib/backup.ts`／`system-center` 相關檔案。 |
| 29 | TypeScript 編譯通過 | 未完成實測 | `node_modules` 不存在，無法執行 `tsc`；已用手動比對修正一處真實型別錯誤（見交付報告「六」）。 |
| 30 | Prisma migration / Production build / Render 正式環境啟動 | migration：沙盒內真實執行（通過）／build／Render：未完成實測 | Migration 已用真實 PostgreSQL 驗證通過；build 與 Render 需要 `npm install` 與網路連線，本沙盒無法執行。 |

## 總結

- **沙盒內真實執行且通過**：8 項（#6, #11, #17, #18, #19, #20, #24, #30 的 migration 部分）。
- **程式碼審查確認邏輯正確**：17 項。
- **未完成實測**（需要瀏覽器/npm建置/正式環境）：約 8 項（部分項目跨兩種方式，如 #15/#16/#30 的 build/Render 部分）。

依工作原則，**本輪不宣稱 V12.0 全部 30 項正式驗收通過**，未完成實測的項目在使用者於有網路/瀏覽器的環境重新驗證前，一律視為待確認狀態。
