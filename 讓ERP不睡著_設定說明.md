# 讓 ERP 不睡著（保持清醒）設定說明

> 最後更新：2026/8/22。**這份文件在 8/22 大改過。**
> 舊版寫的「用 GitHub Actions 每 12 分鐘戳一次」經實測**完全無效**，
> 改良版也經實測**撐不住**。兩種都已放棄，理由與數據見最下面「附錄」。
> 現在只有一個做法：**外部監測服務**。

## 問題
Render 免費方案的網站，**閒置 15 分鐘就會「睡著」**，下次打開要等大約 50 秒才會醒。
解法：在你會用的時段，每隔幾分鐘「戳」網站一下，讓它保持清醒、隨開隨用。

---

## 做法：cron-job.org（唯一推薦）

免費、免信用卡、專門就是做「定時打一個網址」這件事，準時、不延遲、不吃你任何額度。
設定約 3 分鐘。

### 步驟

1. 到 **https://cron-job.org** → 按 **Sign up**，用 Email 免費註冊。
2. 登入後按 **Create cronjob**（建立排程）。
3. 填入：
   - **Title**（標題）：`三玄宮ERP 保持清醒`
   - **URL**：`https://sanxuan-erp-v2.onrender.com/`
4. **執行間隔**：選「Every 5 minutes」（每 5 分鐘）。
   - Render 是閒置 15 分鐘才睡，5 分鐘有三倍安全餘裕。
5. **執行時段**：把時間限制成 **06:00 – 22:00**，星期一到日全選。
   - 時區（Timezone）務必設成 **Asia/Taipei**，不然時段會對不上。
   - 只在上班時段戳，其餘讓它睡，才不會吃掉 Render 免費的每月 750 小時。
6. 存檔。回列表確認狀態是 **Enabled**（啟用）。
7. 過 10 分鐘回來看該筆的 **History**（執行紀錄），應該每 5 分鐘一筆。
   - 網站回 **307** 是**正常的**（首頁會轉到登入頁），不是失敗。

> 介面文字可能與上述略有出入，但「URL、間隔、時段、時區」這四項一定找得到。

### 怎麼確認真的成功

隔天早上 9 點左右直接打開網址，**應該 2 秒內就出現畫面**，而不是轉圈圈等 50 秒。

---

## 另外要做的一件事：把 GitHub 的 keep-awake 停用

`.github/workflows/keep-awake.yml` 這個排程留著也沒用（見附錄），還會每小時
空跑一次。cron-job.org 設好後，請把它停用：

1. 到 GitHub repo → 上方 **Actions** 分頁。
2. 左邊清單點 **Keep ERP awake**。
3. 右上角「**...**」→ **Disable workflow**。

> 用網頁停用即可，**不需要改程式或 push**（改 `.github/workflows/` 底下的檔案
> 需要特殊權限，一般的 token 推不上去）。

---

## 費用說明（不會被偷收錢）

| 項目 | 會不會多收 | 說明 |
|---|---|---|
| Render 網站 | **不會** | 只在 06:00–22:00 保持清醒，月用量約 496 小時，低於免費的 750 小時 |
| cron-job.org | **不會** | 免費方案就夠用，不需信用卡 |

**最壞情況只是「叫醒功能停掉、網站又開始睡」，不會產生費用。**

---

## 如果想「永遠不睡、一開就秒開」（要花錢的選項）
升級 Render 付費方案 **Starter（約 US$7／月）**，24 小時開著，什麼都不用設。
不急的話上面的免費做法就夠。

---

## 附錄：為什麼放棄 GitHub Actions（8/22 實測數據）

### 1. 舊版「每 12 分鐘」：一次都沒跑

`keep-awake.yml` 在 8/22 14:35 推上線，到 15:12（**37 分鐘後**）照設定應觸發
**3 次**，實際觸發 **0 次**。

### 2. 改良版「每小時叫醒、自己撐 75 分鐘」：也撐不住

想法是不依賴 GitHub 準時：cron 每小時觸發一次，工作跑起來後自己每 5 分鐘戳、
連戳 75 分鐘，用執行長度吸收觸發延遲。

這建立在「延遲幅度穩定」的假設上。查了備份排程 **26 次**實際執行後，
發現**這個假設是錯的**——延遲不只長，而且會劇烈抖動：

| 時期 | 延遲範圍 | 最壞空檔 |
|---|---|---|
| 8/15–8/22（狀況好） | 27–56 分 | **19 分** ❌ |
| 8/8–8/14（狀況差） | 59–**150** 分 | **81 分** ❌ |

空檔公式：`空檔 = 下次延遲 − 這次延遲 − 10`。
前一次準時 27 分、下一次拖到 56 分，中間就開天窗。**連狀況最好的時期都差
4 分鐘不及格**，而 8/10–8/13 曾連續四天延遲 150 分鐘。

要補到安全，每次執行得涵蓋約 160 分鐘 → 每天 2560 分鐘 runner 空轉。
那已經不是權宜之計，是把 GitHub 當免費計時器濫用（GitHub 條款明訂 Actions
不該用於「與軟體建置、測試、部署無關的活動」）。

### 3. 好消息：排程「不會被跳過」，只是會延遲

同一批資料顯示，14 天內備份排程該跑的每一次都跑到了（0 次遺漏）。
所以**備份排程本身是可信的**，不用擔心；它只是會晚 30–60 分鐘執行而已，
對「一天備份兩次」這種用途完全沒影響。

### 4. 為什麼不用 UptimeRobot

UptimeRobot 免費版是 **24 小時一直戳**，不能限定 06:00–22:00，會多吃 Render
的免費時數。cron-job.org 可以設執行時段，較合用。
