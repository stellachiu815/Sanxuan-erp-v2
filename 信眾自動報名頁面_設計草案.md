# 信眾自動報名活動頁面 — 設計草案（待明天一起確認後再建）

> 我今晚沒有直接動這個，原因誠實跟你講：這功能要新增一張資料表（存信眾自填的報名），
> 需要跑資料庫遷移（migration）。這種改動如果沒測過就上線到正式站，風險比較高——萬一出錯
> 會影響到整個系統。所以我先把設計寫清楚，明天你確認流程沒問題，我再一次做好、測過才上。
> 這也是我先前說的「先做給你審、測過才對真信眾開放」。

## 你要的流程（我理解的）
1. 廟方在後台**設定一個活動**（例如「115 中元普渡」），系統產生一個**信眾專屬網址**。
2. 把網址發給信眾（LINE／貼公告）。信眾打開就能**自己填報名**（姓名、電話、牌位類別、陽上人、地址等）。
3. 廟方後台看到一份**「待確認」清單**。
4. 廟方核對長輩手寫本後，**點「確認」＝已收到報名**，該筆就從待確認變成已確認。

## 資料設計（新增一張表）
- `PublicRegistration`（信眾自填報名）
  - id、activityId（對應活動）、slug（專屬網址用）
  - 信眾填的欄位：姓名、電話、類別、牌位姓名、陽上人、地址、備註
  - status：`PENDING`（待確認）／`CONFIRMED`（已收到）／`REJECTED`（作廢）
  - createdAt、confirmedAt、confirmedByName
- 活動用既有的 TempleEvent／年度設定掛勾，不另開活動主檔（避免重複）。

## 畫面
1. **公開報名頁**（不需登入）：`/join/[slug]`
   - 只顯示該活動的報名表單，送出後顯示「已收到，等待廟方確認」。
   - 防呆：必填檢查、防重複送出、簡單防灌水（同一裝置短時間內限次）。
2. **後台待確認清單**：`/system-center/public-registrations`
   - 列出 PENDING 的報名，可「確認」「作廢」「編輯」。
   - 確認後可一鍵**轉入正式普渡報名**（或先只標記已收到，之後再匯入）。

## 防呆重點（你一直強調的）
- 信眾自填的資料一律先進「待確認」，**不會自動變成正式牌位**，一定要廟方點確認。
- 陽上人／類別用下拉選單，減少亂填。
- 送出成功給明確回饋，避免信眾重複送。

## 待你明天確認的幾點
1. 報名表單要哪些欄位？（我先抓：姓名、電話、類別、牌位姓名、陽上人、地址、備註）
2. 「確認」之後，要**自動轉成正式普渡報名**，還是先只標記已收到、之後再統一匯入？
3. 一個活動一個網址就好，還是要能同時開多個活動各自的網址？
4. 要不要收費／金額欄位，還是只收報名、金額現場再算？
