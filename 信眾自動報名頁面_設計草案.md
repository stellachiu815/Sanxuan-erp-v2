# 信眾自動報名系統 — 設計（已與 Stella 確認方向，這一期就做）

> 8/6 更新：Stella 決定這一期就上（還有約 23 天報名期，可邊用邊測、對年長宮務人員友善）。
> 分階段做、每步測過，先用測試活動驗證，確認防呆再發網址給信眾。

## 一、確認過的需求
1. **通用、綁活動**：不是只做普渡一頁。以後**每建一個活動，ERP 自動產生一個信眾報名網址**，我複製給信眾即可。
2. **建活動時可勾選報名頁要收哪些欄位**：從**現有信眾資料表的所有欄位**挑（姓名、電話、地址、生日……等），做成勾選清單，因為不同活動要的資料不同。
3. **必填**：報名人姓名、類別、牌位姓名、陽上人。**可勾選加收**：電話、地址、白米／贊普、生日…等（全部信眾欄位）。
4. **顯示各項金額**（參考用）、**不線上付款**（到宮裡再繳）。
5. **信眾填完 → 進「待確認」**；系統整理成**一份可核對的表格**給廟方，方便**對長輩手寫本**；廟方核對後按「確認」＝已收到（確認後可再轉正式）。

## 二、安全上線做法（不用終端機、不動現有資料）
- 新功能一律用**獨立的新資料表**（不改現有活動/信眾表），所以就算表還沒建好，也只有這個新功能暫時無作用，**不影響任何既有功能**。
- 資料表用**一鍵「建立信眾報名資料表」按鈕**建立（系統既有的 raw SQL 做法，純新增、不刪不改、不用終端機）。
- 對外公開頁只收資料進「待確認」，**絕不自動變正式牌位**；有防重複送出、必填檢查、簡單防灌水。

## 三、資料設計（兩張新表）
- `PublicRegForm`（每個活動一份報名表設定）：id、templeEventId、slug（網址用）、fieldsConfig(JSON：要收哪些欄位)、isOpen、createdAt。
- `PublicRegistration`（信眾自填的每一筆報名）：id、formId、狀態(PENDING/CONFIRMED/REJECTED)、payload(JSON：信眾填的內容)、confirmedAt、confirmedByName、createdAt。

## 四、畫面
1. **建活動 → 產生報名網址＋勾選欄位**（後台，接在現有活動建立流程旁）。
2. **公開報名頁** `/join/[slug]`（免登入）：依該活動設定顯示欄位＋金額，送出後顯示「已收到，待廟方確認」。
3. **後台待確認清單／核對表格** `/system-center/public-registrations`：列出 PENDING，可**匯出表格對手寫本**、逐筆「確認／作廢」、確認後可一鍵轉正式。

## 五、分階段（每階段測過再下一步）
1. **地基**：兩張新表 + 一鍵建表按鈕（安全、可回復）。
2. **後台**：建活動勾選欄位 + 產生網址 + 待確認清單/匯出。
3. **公開頁**：`/join/[slug]` 報名表單（動態欄位＋金額）。
4. **測試**：用一個測試活動走完整流程，確認防呆，再對真信眾開放。

## 六、待你有空時再確認的小細節
- 生日等敏感欄位要不要設成「選填」而非必填？
- 「確認後轉正式」是自動、還是你再按一顆「轉入正式普渡」？
